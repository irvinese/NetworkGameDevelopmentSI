# Auto detect text files and perform LF normalization
* text=auto

# Specific file types can have forced line endings
*.sh eol=lf   # Shell scripts use Unix (LF) line endings
*.bat eol=crlf # Batch scripts use Windows (CRLF) line endings

# Treat binary files as-is
*.jpg binary
*.png binary
*.pdf binary